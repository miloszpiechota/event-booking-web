export const formatLocalization = (
    street: string,
    apartment_number: string,
    city: string,
    country: string
  ) => {
    return `${street} ${apartment_number} ${city} ${country}`;
  };
  